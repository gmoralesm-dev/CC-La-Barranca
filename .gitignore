# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase
.firebase/

# AI agent workspace files
.agents/
skills-lock.json

# Logs
*.log

# Backups
backup/

# Secrets - never commit these. Note that git tooling (e.g. Cline
# checkpoints) snapshots untracked files too, so secrets must not live in
# the working tree at all - keep them in a password manager.
*recovery-codes*
*-recovery-codes.txt
.env

# Local scratch / agent session files
diff_app.txt
diff_reports.txt
resume

